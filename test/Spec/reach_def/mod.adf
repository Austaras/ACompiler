fn calc(mut x: int) {
    while x > 1 {
        let y = x / 2

        if y > 3 {
            x = x - y
        }

        let mut z = x - 4

        if z > 0 {
            x = x / 2
        }

        z = z - 1
    }

    x
}

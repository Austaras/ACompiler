pub struct RawVec<T> {
    ptr: uint,
    len: uint,
    cap: uint
}

impl<T> RawVec<T> {
    #[compiler_builtin]
    pub fn malloc(size: uint) -> Self {
    }

    #[compiler_builtin]
    pub fn realloc(&self, size: uint) {
    }
}
